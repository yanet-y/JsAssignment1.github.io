var a,b,c;

function areaOfTriangle(a,b,c){
    area = (a * b) / 2; //assuming a and b are the base and height of the triangle respectively
    console.log("The area of the triangle is:", area)
     
}

areaOfTriangle(5,6,7);

var newString
function addString(string) {
    newString= "JavaScript " + string;
    console.log(newString);
    
}
  
addString("is useful for building websites");
  
  
  
